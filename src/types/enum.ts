export enum DeleteEnum {
  NOT_DELETE,
  DELELTE,
}

export enum DoneEnum {
  UNDONE,
  DONE,
}

export enum SaveEnum {
  SAVE,
  EDIT,
}

export enum ThemeEnum {
  LIGHT = "light",
  DARK = "dark",
  SYSTEM = "system",
}
